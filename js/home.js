/**
 * js/home.js — lógica exclusiva de index.html
 * (Por ahora el progreso de mundo es estático de ejemplo;
 *  cuando exista el sistema de campaña real, aquí se calcula desde State.)
 */
document.addEventListener("DOMContentLoaded", () => {
  // El topbar (gemas/oro) ya se actualiza solo desde core/state.js
});
